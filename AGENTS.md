# プロジェクト全体開発・デザインルール (AGENTS.md)

## 🎨 フロントエンド・UI デザイン規約 (Anti-AI-Slop / Hallmark 準拠)

本プロジェクトでは、AI特有の安っぽいテンプレート感（いわゆる「AIスロップ」）を徹底的に排除し、プロフェッショナルで信頼性の高いプロダクトUIを構築します。

### 1. 絵文字 (Emoji) の原則禁止
- **禁止事項**: 見出し、ボタン、タブ、カード、バッジ等に絵文字（例: ⚔️, 📊, 📜, 🚨, 🏆, 💡, 🥇, 🥈, 🛒, ⚠️, ✏️, 🎯 など）をアイコン代わりに多用すること。
- **推奨代替**:
  - アイコンが必要な場合は、**Lucide React などの洗練されたモノトーン/クリーンなSVGアイコン**（14〜16px程度で控えめに配置）を使用する。
  - アイコンに頼らず、**タイポグラフィのジャンプ率、フォントウェイト（bold / medium）、適切な余白、洗練されたバッジ**で情報の階層とステータスを表現する。

### 2. カラーパレットとデザイントークン
- **基調**: Google風クリーンデザイン（背景: `#f8f9fa`, カード背景: `#ffffff`, 境界線: `#dadce0`, テキスト主色: `#202124`, サブテキスト: `#5f6368`）。
- **ステータスカラー**:
  - 勝利/推奨: `#1a73e8` (アクセント青) / 背景 `#e8f0fe` / 境界線 `#d2e3fc`
  - 敗北/要警戒: `#d93025` (警告赤) / 背景 `#fce8e6` / 境界線 `#fad2cf`
  - 成功/得意: `#137333` (成功緑) / 背景 `#e6f4ea` / 境界線 `#b7e1cd`
  - 注意/Hard: `#b06000` (注意黄) / 背景 `#fef7e0` / 境界線 `#fce8b2`
- 原色や過度な紫/ピンクのグラデーション、派手すぎるネオンカラーは使用しない。

### 3. レイアウトとタイポグラフィ
- 画一的なカードの3等分並べ（AI特有のテンプレ）を避け、重要度（天敵や直前の試合など最優先情報）に強弱をつける。
- 意味のないバウンスや過度なアニメーション（ポヨンポヨン動くエフェクト）を排除し、静かで高速、確実なレスポンスを提供する。

## 💎 バックエンド・Ruby 設計規約 (Anti-AI-Hack / Clean Architecture)

本プロジェクトでは、AI特有の「動けばいいや」という対症療法的なパッチや不適切な裏技を排除し、初見の開発者でも素直に読める保守性の高いコードを維持します。

### 1. 責務の明確化と純粋関数の設計
- 外部APIクライアントやService層において、インスタンス状態（APIキーやHTTP通信）に依存しない純粋な判定・変換ロジック（例: リージョン判定、タグ変換、計算処理）は、**クラスメソッド（`def self.xxx`）** やユーティリティとして切り出し、単体テストを必ず用意する。
- 外部から呼び出したい処理がある場合は、場当たり的な抜け道を探すのではなく、適切な公開インターフェース（`public` メソッドまたはクラスメソッド）として堂々と設計する。

### 2. テストにおけるカプセル化保護（振る舞いテストの原則）
- **private メソッドの直接テスト禁止**: テストであっても `send` や `__send__` で private メソッドを直接呼び出さない（RuboCop `Custom/NoPrivateSend` で機械的にブロック）。
- **公開インターフェース経由の検証**: 内部の private 実装を直接テストするのではなく、利用者が実際に呼び出す公開メソッド（`public` メソッド）の振る舞いを通じて、エラーハンドリングやリトライの動作を検証する。


## 🛡️ 静的解析・型チェックを活用した開発フロー規約

構文やスタイルの細かなルールは、自然言語の規約で二重管理せず、**Lint（RuboCop / ESLint）および型チェック（TypeScript）に一任**します。AIエージェントは以下の開発プロセスを遵守してください。

### 1. 変更後の自動検証の徹底
- コード変更を行った後は、必ずプロジェクトの静的解析・型チェックを実行し、エラーや警告がゼロであることを確認する。
  - **フロントエンド**: `npx eslint .` / `npx tsc --noEmit`
  - **バックエンド**: RuboCop による静的解析

### 2. Lint指摘に対する根本解決の原則
- Lintで警告やエラーが検出された際、コメントによるルール無効化（例: `# rubocop:disable ...`, `eslint-disable ...`）や、検出をすり抜けるための奇異なコードで安易に握りつぶさない。
- なぜそのルールが存在するのか（カプセル化、安全性、可読性）を理解し、**クラス設計やシグネチャの改善による根本解決**を行う。
